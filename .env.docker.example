# =====================================================
# PGNexus Docker Environment Configuration
# =====================================================
# Copy this file to .env and update with your values
# =====================================================

# -----------------------------------------------------
# Application Configuration
# -----------------------------------------------------
PORT=3000
HOSTNAME=0.0.0.0
NODE_ENV=production

# -----------------------------------------------------
# Database Configuration (Option 1: Individual Components)
# -----------------------------------------------------
# These will be automatically combined into DATABASE_URL
PG_HOST=172.23.0.2
PG_PORT=5432
PG_USER=n8n-user
PG_PASSWORD=mypass
PG_DB=n8n-db
PG_SCHEMA=public

# -----------------------------------------------------
# Database Configuration (Option 2: Full Connection String)
# -----------------------------------------------------
# Uncomment and use this instead of individual components above
# DATABASE_URL=postgresql://pgnexus:your_secure_password_here@postgres:5432/pgnexus?schema=public

# -----------------------------------------------------
# NextAuth Configuration
# -----------------------------------------------------
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32
NEXTAUTH_URL=http://localhost:3000

# For production, set NEXTAUTH_URL to your public domain:
# NEXTAUTH_URL=https://yourdomain.com

# -----------------------------------------------------
# Optional Configuration
# -----------------------------------------------------
# Skip database migrations on startup (default: false)
# SKIP_MIGRATIONS=false

# Enable Next.js telemetry (default: disabled)
# NEXT_TELEMETRY_DISABLED=1
